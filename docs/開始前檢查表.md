# 開始前檢查表

## 電腦端

- [ ] Vivado 可啟動，版本與課程記錄一致。
- [ ] New Project 的 Parts 頁能找到 `xc7z020clg400-1`。
- [ ] 若要用 Block Automation，Boards 頁能找到 PYNQ-Z2。
- [ ] 專案路徑只使用英數、底線，且路徑不要太深。
- [ ] Python 可執行，`python --version` 與虛擬環境已記錄。
- [ ] 至少有足夠空間保存四組 Vivado run；建議保留 30 GB 以上工作空間。

## 板卡端

- [ ] microSD 已燒錄 PYNQ image。
- [ ] Boot jumper 位於 SD。
- [ ] 使用 USB 供電時，Power jumper 位於 USB。
- [ ] 網路可連到 `http://192.168.2.99` 或板卡取得的 DHCP IP。
- [ ] Jupyter 中可執行 `from pynq import Overlay, allocate`。
- [ ] 知道如何安全關機，不直接在寫入 SD 卡時拔電源。

## 專案檔案

- [ ] 已讀 `ASSUMPTIONS.md`，硬體與手上設備一致。
- [ ] 已建立自己的結果資料夾，不直接覆蓋範例／模板。
- [ ] Design Sources 與 Simulation Sources 分開加入。
- [ ] top module 名稱與 Tcl、XDC 內設定一致。
- [ ] 每次修改介面後，都重新跑一次 Testbench。

